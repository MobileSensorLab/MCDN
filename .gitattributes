*.pt filter=lfs diff=lfs merge=lfs -text

# Cluster-side shell scripts must land with LF endings regardless of checkout platform;
# CRLF in an sbatch/bash script breaks execution on the SLURM nodes.
*.sh text eol=lf
*.sbatch text eol=lf
